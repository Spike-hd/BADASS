frr defaults traditional
hostname hduflos-routeur1
service integrated-vtysh-config
!
! No routing protocol is needed for Part 2: the two routers are directly
! connected on the underlay, so zebra's built-in connected routes are enough.
! All VXLAN and bridge configuration is done in start.sh via iproute2.
!
line vty
!
